#include <gtest/gtest.h>

// #include "CSVTable.h"  // CSVTable 클래스 헤더

TEST(CSVTableTest, LoadCSV)
{
    // CSVTable table;
    // bool result = table.Load("some_file.csv");
    // EXPECT_TRUE(result);
    SUCCEED();
}

TEST(CSVTableTest, ReadValue)
{
    // CSVTable table;
    // table.Load("some_file.csv");
    // EXPECT_EQ(table.GetValue(0, 0), "expected_value");
    SUCCEED();
}
