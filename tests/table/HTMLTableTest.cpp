#include <gtest/gtest.h>

// #include "HTMLTable.h"

TEST(HTMLTableTest, GenerateHTML)
{
    // HTMLTable table;
    // std::string html = table.Generate();
    // EXPECT_FALSE(html.empty());
    SUCCEED();
}
